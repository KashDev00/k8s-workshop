[defaults]
inventory = ./inventory_all.ini
host_key_checking = False
remote_user = ubuntu
# Private key is defined in the inventory [all:vars], but we can set a default here if needed.
# private_key_file = ~/.ssh/id_rsa
deprecation_warnings = False
command_warnings = False
